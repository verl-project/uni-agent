- name: swe_bench
  sandbox:
    provider: vefaas
    runtime_timeout: 7200
    image_map:
      - from: "swebench/**:latest"
        to: "enterprise-public-cn-beijing.cr.volces.com/swe-bench-verified/**:v2"
      - from: "swerebench/**:latest"
        to: "enterprise-public-cn-beijing.cr.volces.com/swe-rebench/**:latest"
  agent:
    name: claude_code
    max_turns: 200
    run_timeout: 4800
    model:
      temperature: 1.0
      top_p: 1.0
      max_total_tokens: 131072
  prompt_template: &swe_claude_prompt
    - role: system
      content: You are a software engineer working in an existing repository.
    - role: user
      content: |-
        <uploaded_files>
        /testbed
        </uploaded_files>
        Inspect the repository in /testbed and resolve the following issue:

        <issue_description>
        {problem_statement}
        </issue_description>

        Understand the relevant code and expected behavior before editing. Implement the smallest correct change in non-test files, preserve existing behavior outside the issue, and follow the repository's established patterns. Do not modify existing tests. Validate the change with focused reproduction steps and the relevant test suite before finishing.

- name: swe_rebench
  sandbox:
    provider: vefaas
    runtime_timeout: 7200
    image_map:
      - from: "swebench/**:latest"
        to: "enterprise-public-cn-beijing.cr.volces.com/swe-bench-verified/**:v2"
      - from: "swerebench/**:latest"
        to: "enterprise-public-cn-beijing.cr.volces.com/swe-rebench/**:latest"
  agent:
    name: claude_code
    max_turns: 200
    run_timeout: 4800
    model:
      temperature: 1.0
      top_p: 1.0
      max_total_tokens: 131072
  prompt_template: *swe_claude_prompt

- name: swe_bench_multilingual
  sandbox:
    provider: vefaas
    runtime_timeout: 7200
  agent:
    name: claude_code
    max_turns: 200
    run_timeout: 4800
    model:
      temperature: 1.0
      top_p: 1.0
      max_total_tokens: 131072
  prompt_template:
    - role: system
      content: You are a software engineer working in an existing repository.
    - role: user
      content: |-
        <uploaded_files>
        /testbed
        </uploaded_files>
        Inspect the repository in /testbed (primary language: {language}) and resolve the following issue:

        <issue_description>
        {problem_statement}
        </issue_description>

        Understand the relevant code and expected behavior before editing. Implement the smallest correct change in non-test files, preserve existing behavior outside the issue, and follow the repository's established patterns. Do not modify existing tests. Validate the change with focused reproduction steps and the relevant test suite before finishing.
